node_modules/
.venv/
__pycache__/
*.pyc
.env
snapshots.db
snapshots.db-shm
snapshots.db-wal
dist/
*.db-journal
.claude/
history.txt
data/
backtest_*.json

# Phase 6 cleanup: runtime DBs and ad-hoc JSON dumps
flow_alerts.db
king_breakouts.db
king_migrations.db
zero_dte_alerts.db
floor_migrations.db
structural_turns.db
newsletters.db
*_dump.json
live_*.json
setup_cooldown.json
aaoi*.json
nvda.json
oklo.json

# Apr 30 — script-local caches and per-run artifacts
scripts/.*.db
.gex_backfill_checkpoint.json
gex_backfill.log
paired_trades.db

# May 7-8 session: research DBs (large binary, regenerate from scripts)
unified_setup_backtest.db
realistic_slippage_backtest.db
ema_cross_backtest_6mo.db
shadow_alerts.db
paper_executions.db

# OAuth token caches (sensitive)
.etrade_tokens.json

# Jun 15 — runtime artifacts that slipped through (same policy as snapshots.db)
logs/
alert_outcomes.db
alert_outcomes_backup_*.BAK
*.BAK
ema_8_9_21_backtest.db
_screenshots/
/1
qqq.json
qqq_meta.json
qqq_now.json
thetav3.json
thetav3-greeks.txt

# Direction A GEX-spine backtest working artifacts (regenerate from scripts)
gex_backtest/

# Jun 28 housekeeping — keep ad-hoc dumps out of the repo ROOT (root-anchored
# so archived/ copies under archive/ are not re-flagged). See PROJECT_STRUCTURE.md.
/grid_*.json
/mir_*.json
/walk_forward_results.json
/spy_intraday_results.json
/king_compression_*.csv
# archived screenshots (were _screenshots/, now under archive/) — keep images out of git
archive/screenshots/
gex_backtest.db

# Thread infographics — rendered PNGs regenerate from the committed SVGs
thread_assets/*.png

# Throwaway per-pattern backtest scripts written by workflow agents (findings
# docs capture the results; the named harness scripts are the durable artifacts)
scripts/gex_bt/_*.py

# research-loop output artifacts (regenerated; docs capture the findings)
research/results/
